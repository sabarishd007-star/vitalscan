# Comma-separated production frontend origins. No wildcards.
APP_ENV=production
ALLOWED_ORIGINS=https://app.yourdomain.com
# Set true only after the deployed model has been independently validated and approved.
MODEL_VALIDATED=false
