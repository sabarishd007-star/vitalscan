# Regenerable synthetic distillation datasets (see generate_synthetic_dataset.py)
dist_dataset/
dist_eval/
dist_eval2/

# Real-face robustness corpus (download_real_faces.py re-fetches these;
# manifest.json IS committed, the photographs themselves are not redistributed)
real_faces/images/

# Local report-history store (app/services/report_store.py) — use Supabase for
# shared persistence; this file is the zero-config development fallback.
data/
